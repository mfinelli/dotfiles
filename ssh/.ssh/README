SSH keys

Keys generated: `ssh-keygen -t ed25519 -o -a 128 -C username -f ./file`
Check randomart: `ssh-keygen -lv -f ./file`

For services that don't (yet) support ed25519 use rsa:
`ssh-keygen -t rsa -b 4096 -o -a 128 -C username -f ./file`
