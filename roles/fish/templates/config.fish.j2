# {{ ansible_managed }}

set -gx EDITOR (type -p vim)

if status --is-login
    if test -d $HOME/bin
        fish_add_path $HOME/bin
    end
end

if status is-interactive
    mp init | source
end

# vim: ft=fish.jinja2
